This extension add a "Reflow Paragraph" that will rewrap a paragraph to fit within 80 characters.
It supports the following comment styles:
- Block comments using `/*` and `*/`.
- Comments starting with `//`
